#Dependencies

1. BeautifulSoup4

Installation For windows on cmd prompt: 
    pip install beautifulsoup4

2. lxml

Installation For windows on cmd prompt: 
    pip install lxml

3. requests

Installation For windows on cmd prompt: 
    pip install requests